# Keep Firestore security rules out of the public repo — they're managed
# directly in the Firebase console (Firestore Database > Rules) instead.
firestore.rules

# PII and admin credentials — never publish these. Used only locally to
# seed Firestore's private "roster" collection.
private-roster-data.json
scripts/service-account-key.json
node_modules/

.DS_Store
SETUP.md
